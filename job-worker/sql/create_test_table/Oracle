CREATE USER TESTDB IDENTIFIED BY Oradoc_db1

create table TESTDB.student (
 id NUMBER GENERATED BY DEFAULT AS IDENTITY,
 name VARCHAR(100),
 address VARCHAR(100),
 age NUMBER,
 birthday timestamp,
 gender NUMBER,
 average_score REAL,
 email VARCHAR(100)
)