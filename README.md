
*NOTE* it's a test version. 
To start our docker:
First (only 1 time), create the network:
`docker network create di_dev_network`
Last, start our docker using docker-compose
`docker-compose up -d`

Now you could access to the rocket-web-bi on port 5050:
http://localhost:5050/

