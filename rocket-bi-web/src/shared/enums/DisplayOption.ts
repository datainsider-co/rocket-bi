export enum DisplayOption {
  onlyLegend = 'Only legend',
  legendValue = 'Legend value',
  legendPercent = 'Legend percent',
  insideValue = 'Inside value',
  insidePercent = 'Inside percent',
  outsideValue = 'Outside value',
  outsidePercent = 'Outside percent'
}
