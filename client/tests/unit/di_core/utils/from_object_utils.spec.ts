describe('Parse object to correct domain', () => {
  const commonSetting = { id: 1, name: '', description: '' };
  //
  // it('should return correct filter', () => {
  //   const equal = new Equal(new Field('dbTest', 'transaction', 'created_date', 'string'), 'not yet set');
  //   const like = new Like(new Field('dbTest', 'transaction', 'created_date', 'string'), 'not yet set');
  //   const dateFilter = new DateFilter(commonSetting);
  //   const dateFilterObj = JSON.parse(JSON.stringify(dateFilter));
  //   Log.debug('=============================================================');
  //   parsedDateFilter.setDate('mot ngay dep troi');
  //   parsedInputFilter.setValue('mot con vit xoe ra hai cai canh');
  //   const parsedEqual = parsedDateFilter.toCondition(1) as Equal;
  //   const parsedLike = parsedInputFilter.toCondition(2) as Like;
  //   expect(parsedEqual instanceof Equal).eq(true);
  //   expect(parsedEqual.value).eq('mot ngay dep troi');
  //   expect(parsedLike instanceof Like).eq(true);
  //   expect(parsedLike.value).eq('mot con vit xoe ra hai cai canh');
  // });
});
